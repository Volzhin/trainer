/// <reference path="../pb_data/types.d.ts" />

/*
 * Проверка снята.
 *
 * Здесь стоял временный ответ `/api/whoami`, показывавший, каким сервер видит
 * адрес клиента. Он был нужен один раз — убедиться перед включением лимитов,
 * что доверенный заголовок доходит и что подделать его снаружи нельзя.
 *
 * Что он показал: сервер видит публичный адрес клиента, а не адрес nginx, и
 * подставленный в запрос X-Forwarded-For результат не меняет — nginx
 * дописывает свой взгляд справа, а PocketBase берёт именно его.
 *
 * Файл оставлен пустым намеренно, а не удалён: деплой выкладывает каталог
 * обработчиков через rsync без --delete, и удалённый из репозитория файл
 * остался бы лежать на сервере и отвечать.
 */
