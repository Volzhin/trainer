/// <reference path="../pb_data/types.d.ts" />

/*
 * Обработчик снят.
 *
 * Здесь стояла проверка входящих записей обмена: ключ строки против rid и
 * владелец записи против того, кому строка принадлежит по содержимому. После
 * её выкладки обмен встал у всех сразу — ни назначения, ни сообщения не
 * доходили ни в одну сторону, — и первым делом снимаем её, а не ищем причину
 * на живых людях.
 *
 * Файл оставлен пустым намеренно, а не удалён: деплой выкладывает каталог
 * обработчиков через rsync без --delete, и удалённый из репозитория файл
 * остался бы лежать на сервере и работать. Пустой — перезапишет.
 *
 * Сама проверка никуда не делась: она с самого начала стоит на устройстве, в
 * apply (src/db/sync.ts), и подменённую запись отбрасывает там. Серверная
 * была вторым рубежом. Возвращать её стоит только после того, как будет
 * понятно, что именно ломалось, и с проверкой на тестовом стенде — правило,
 * останавливающее обмен, опаснее того, от чего оно защищает.
 */
